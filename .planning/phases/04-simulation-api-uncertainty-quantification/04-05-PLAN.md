---
phase: 04
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - packages/api/tests/test_simulate.py
  - packages/api/tests/test_phase4_integration.py
autonomous: false
requirements: [API-04, API-05, API-06]
user_setup: []
tags: [integration, benchmark, human-verify]

must_haves:
  truths:
    - "test_simulate_wall_time benchmark runs on a realistic (~22-lap) fixture and asserts cold-path wall time is below the D-04 2 s budget (stub-profiled, not Jolpica)"
    - "End-to-end integration test: upload session -> simulate -> calibration summary -> all three endpoints return 200 with correctly-shaped payloads"
    - "Full Phase 4 test suite is green: 19 unit tests + 3 integration tests"
    - "Human operator runs the app locally, posts a real request, and confirms visually that the response contains three-level data + CI triplets + metadata"
  artifacts:
    - path: "packages/api/tests/test_simulate.py"
      provides: "test_simulate_wall_time implementation (benchmark)"
    - path: "packages/api/tests/test_phase4_integration.py"
      provides: "test_e2e_upload_simulate_calibration covering API-04+05+06 together"
  key_links:
    - from: "test_simulate_wall_time"
      to: "services/simulate.py run_simulation_with_uncertainty"
      via: "pytest-benchmark harness with monkeypatched run_simulation returning a canned tiny SimulationResult"
      pattern: "benchmark"
    - from: "test_e2e_upload_simulate_calibration"
      to: "all 3 Phase 4 routers"
      via: "TestClient POSTs and GETs in sequence"
      pattern: "client.post or client.get"
---

<objective>
Wave-3 verification: run the remaining integration tests (test_simulate_wall_time and a new end-to-end test spanning all three Phase 4 endpoints), then pause for a human-verify checkpoint where the user confirms the app behaves correctly when run via `uvicorn`.

This is the last Phase 4 plan. After it completes, all 19 per-requirement tests + 3 integration tests + the human checkpoint are green, and Phase 4 is done.

Scope EXCLUDES:
- Any new production code (Plans 01-04 implemented everything; this plan only adds tests + checkpoint).

Purpose: Nyquist compliance gate. Without running the wall-time benchmark, the <2 s budget from D-04 is unverified. Without the E2E test, regressions in the 3-endpoint interaction surface are undetectable.

Output: 2 test file updates + 1 human checkpoint -> Phase 4 verification complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-simulation-api-uncertainty-quantification/04-CONTEXT.md
@.planning/phases/04-simulation-api-uncertainty-quantification/04-RESEARCH.md
@.planning/phases/04-simulation-api-uncertainty-quantification/04-VALIDATION.md
@packages/api/src/f1_api/app.py
@packages/api/src/f1_api/services/simulate.py
@packages/api/src/f1_api/services/calibration.py
@packages/api/src/f1_api/services/sessions.py
@packages/api/tests/conftest.py
@packages/api/tests/fixtures/calibration_fixture.py
@packages/api/tests/fixtures/zip_fixtures.py

<interfaces>
The canonical fixture (Phase 1) is Bahrain 2023 VER stint 2, ~22 laps.
The benchmark uses a MONKEYPATCHED run_simulation that returns a synthetic SimulationResult with arrays of shape (8000, 4) for per-timestep (roughly 4 Hz x 30 laps x 90s).

pytest-benchmark is declared in Plan 00 (packages/api/pyproject.toml [project.optional-dependencies].test).

For the E2E test, we reuse monkeypatches from Plan 01 (load_stint, run_simulation, read_latest_calibration_run, get_posterior, sample_stage4_draws) + Plan 03 (SESSION_ROOT).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wall-time benchmark + E2E integration test</name>
  <read_first>
    - packages/api/tests/test_simulate.py (current state of test_simulate_wall_time — Wave-0 skip)
    - packages/api/src/f1_api/services/simulate.py (run_simulation_with_uncertainty signature + K_DRAWS constant)
    - packages/core/src/f1_core/physics/orchestrator.py (SimulationResult shape for building synthetic fixtures)
    - packages/api/tests/conftest.py (client, fixture_calibration, malicious_zip fixtures)
    - .planning/phases/04-simulation-api-uncertainty-quantification/04-RESEARCH.md Architecture Patterns 2 (timing budget breakdown: NetCDF 5 ms, JSON 50 ms, aggregation 50 ms, K draws ~1.8 s)
  </read_first>
  <files>
    packages/api/tests/test_simulate.py,
    packages/api/tests/test_phase4_integration.py,
    packages/api/tests/conftest.py
  </files>
  <behavior>
    - `test_simulate_wall_time` (@pytest.mark.integration): monkeypatches `run_simulation` to return a synthetic SimulationResult with realistic shape (N=8000 timesteps, 30 laps) in <1 ms per call. K=100 calls run through the real aggregation + JSON serialization + cache write. Asserts: total_wall_time < 2.0 seconds on cold path.
    - `test_e2e_upload_simulate_calibration` (new file, @pytest.mark.integration):
      1. Use fixture_calibration to seed DB + NetCDF.
      2. POST /sessions/upload with make_valid_zip() -> 200 + session_id.
      3. POST /simulate with session_id -> 200 + SimulateResponse.
      4. GET /calibration/C3 -> 200 + CalibrationResponse with calibration_id matching the /simulate response metadata.
      5. Cross-check: /simulate response metadata.calibration_id == /calibration response metadata.calibration_id.
  </behavior>
  <action>
    **Step 1 - Extract stubbing block into conftest.py**

    Add to `packages/api/tests/conftest.py` the helper function `install_simulate_stubs(monkeypatch, fixture_calibration, tmp_path)`. This helper installs the same stubs both tests need: stubs `run_simulation`, `load_stint`, `_build_params_list`, `_derive_compound_letter`, `get_posterior`, `sample_stage4_draws`, `read_latest_calibration_run`, and sets DEFAULT_DB_PATH. The helper returns the calibration_id so tests can assert on it.

    The stub bodies:
    - `_fake_run_simulation(artifact, params) -> SimulationResult` returns a SimulationResult with:
      N = 8000, t = np.linspace(0, 2000, N), f_z/f_y/f_x/mu/t_tread/e_tire = np.random.uniform over (N,4) with physically plausible ranges, mu_0 = np.linspace(1.2, 1.0, N), per_lap = list of 22 dicts with keys Lap, Compound='MEDIUM', Age, Pred_s, Obs_s, Delta_s, Grip_pct, T_tread_C, E_tire_MJ; events=[].
    - `_fake_load_stint(year, event, driver_code, stint_index) -> StintArtifact` with minimal pandas DataFrames: laps has Compound and LapTime columns (22 rows); car_data has Time and Speed (4 rows); pos_data + weather = empty.
    - `_fake_build_params_list(compound, draws, overrides)` returns `[make_nominal_params() for _ in range(100)]`.
    - `_fake_sample_stage4_draws(idata, K, seed)` returns `{"beta_therm": np.ones(K)*1e-6, "T_act": np.ones(K)*100.0, "k_wear": np.ones(K)*1e-10}`.
    - `_fake_read_latest_calibration_run(db, compound)` returns `{"calibration_id": fixture_calibration[1], "netcdf_path": str(fixture_calibration[0])}`.

    All stubs applied via monkeypatch.setattr pointing at the `f1_api.services.simulate` module attributes.

    Also set module-level cache singleton to None via `f1_api.services.simulate._cache = None` to force re-init against the tmp path DB. And monkeypatch `f1_api.services.simulate.DEFAULT_DB_PATH` to `tmp_path / "cache.db"`.

    **Step 2 - Replace test_simulate_wall_time skip with real benchmark** in `packages/api/tests/test_simulate.py`:

    The test calls install_simulate_stubs, then:
        import time
        from f1_api.services.simulate import run_simulation_with_uncertainty

        t0 = time.perf_counter()
        resp = run_simulation_with_uncertainty(
            race_id="2023-bahrain_grand_prix",
            driver_code="VER",
            stint_index=2,
            overrides=None,
            session_id=None,
        )
        elapsed = time.perf_counter() - t0
        assert elapsed < 2.0, f"cold /simulate took {elapsed:.3f}s (budget 2.0s)"
        assert resp.metadata.calibration_id == fixture_calibration[1]
        assert resp.metadata.k_draws == 100

    Keep the `@pytest.mark.integration` decorator.

    **Step 3 - Create packages/api/tests/test_phase4_integration.py**:

    Module docstring:
        """Phase 4 cross-endpoint integration test.
        Exercises /sessions/upload -> /simulate -> /calibration/{compound} in one flow
        to catch regressions in the 3-endpoint interaction surface.
        """

    Single test function `test_e2e_upload_simulate_calibration(client, monkeypatch, tmp_path, fixture_calibration)`:
        @pytest.mark.integration

        1. Call install_simulate_stubs(monkeypatch, fixture_calibration, tmp_path).
        2. Also monkeypatch f1_api.services.sessions.SESSION_ROOT to tmp_path / "sessions" AND f1_api.services.simulate.SESSION_ROOT to the same.
        3. Also monkeypatch f1_api.services.calibration.DEFAULT_DB_PATH to fixture_calibration[2].
        4. Import make_valid_zip from tests.fixtures.zip_fixtures (add sys.path setup if needed).
        5. POST /sessions/upload with files={"file": ("cache.zip", make_valid_zip(), "application/zip")}. Assert status == 200, read session_id from body.
        6. POST /simulate with JSON {"race_id": "2023-bahrain_grand_prix", "driver_code": "VER", "stint_index": 2, "session_id": session_id}. Assert status == 200.
        7. Parse body: assert "metadata", "per_timestep", "per_lap", "per_stint" all present. Assert body["metadata"]["calibration_id"] == fixture_calibration[1].
        8. GET /calibration/C3. Assert status == 200.
        9. Parse: assert cal_body["metadata"]["calibration_id"] == sim_body["metadata"]["calibration_id"].
  </action>
  <verify>
    <automated>cd packages/api && pytest tests/test_simulate.py::test_simulate_wall_time tests/test_phase4_integration.py::test_e2e_upload_simulate_calibration -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api/tests/test_simulate.py::test_simulate_wall_time` is @pytest.mark.integration, no longer a skip (grep `pytest.skip` inside test_simulate_wall_time body returns 0)
    - The benchmark asserts `elapsed < 2.0` (grep `< 2.0` returns at least 1 match)
    - File `packages/api/tests/test_phase4_integration.py` exists with one test `test_e2e_upload_simulate_calibration` marked `@pytest.mark.integration`
    - E2E test POSTs to /sessions/upload, /simulate, and GETs /calibration/C3 (grep `client.post` returns at least 2 matches, `client.get` returns at least 1 match)
    - E2E test asserts all 3 calibration_ids match (grep `calibration_id` in test file returns at least 3 matches)
    - `conftest.py` contains `install_simulate_stubs` helper (grep `def install_simulate_stubs` returns 1 match)
    - Running `pytest tests/test_simulate.py::test_simulate_wall_time tests/test_phase4_integration.py -m integration -v` passes
  </acceptance_criteria>
  <done>
    test_simulate_wall_time asserts <2 s on the stubbed K=100 path. Integration test covers 3-endpoint interactions and cross-asserts calibration_id consistency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full Phase 4 test suite regression run</name>
  <read_first>
    - All Phase 4 tests (test_simulate.py, test_calibration.py, test_sessions.py, test_phase4_integration.py)
    - packages/api/tests/test_endpoints.py (Phase 1 tests — must still pass)
  </read_first>
  <files>packages/api/tests/</files>
  <action>
    Run the full Phase 4 test suite and record the results.

    Expected counts (unit + integration):
    - test_endpoints.py (Phase 1): N passing (whatever Phase 1 had — no regression)
    - test_simulate.py: 6 unit passing + 2 integration passing = 8 total green
    - test_calibration.py: 5 unit passing = 5 total green
    - test_sessions.py: 5 unit + 1 integration = 6 total green (plus bonus test_upload_rejects_symlink)
    - test_phase4_integration.py: 1 integration passing

    Grand total new in Phase 4: 19 unit + 3 integration + 1 bonus symlink = 23 tests; all green.

    If any fails, STOP and diagnose before the checkpoint task. A red test here means upstream plan (00-04) has a latent bug.

    Record the results in the summary artifact: paste the tail of `pytest --tb=no -q` output into a scratchpad for the checkpoint task.
  </action>
  <verify>
    <automated>cd packages/api && pytest tests/ -v --tb=line 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pytest packages/api/tests/ -m "not integration"` — 0 failures (all unit tests green)
    - `pytest packages/api/tests/ -m integration` — 0 failures (all integration tests green)
    - `pytest packages/api/tests/ -v --tb=no -q 2>&1 | grep -E "failed|error" | wc -l` returns 0
    - The tail of the test output shows at least 22 passed (19 + 3 integration + bonus) across Phase 4 tests
    - Phase 1 `test_endpoints.py` still passes (no regressions)
    - `test_no_mcmc_at_runtime` is in the passing list (D-05 enforcement holds end-to-end)
  </acceptance_criteria>
  <done>
    Full Phase 4 suite is green. Ready for human-verify checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify - run the app locally and confirm the /simulate response</name>
  <read_first>
    - .planning/phases/04-simulation-api-uncertainty-quantification/04-CONTEXT.md (what the user locked — D-01..D-09)
    - packages/api/src/f1_api/app.py (final app shape)
  </read_first>
  <files>NONE (read-only verification)</files>
  <action>
    This is a human-verify checkpoint. Claude presents the verification procedure to the user; the user runs each step manually and confirms the outputs. See `<how-to-verify>` below for the step-by-step procedure. Claude MUST NOT execute these steps automatically — they require a running uvicorn server and human judgment on response payloads.

    After the user types "approved", Claude proceeds to write the summary. After any "issue" signal, Claude debugs the specific reported step.
  </action>
  <what-built>
    All three Phase 4 endpoints: POST /simulate, GET /calibration/{compound}, POST /sessions/upload. The app is wired with GZipMiddleware and a session TTL cleanup daemon. Posterior NetCDFs are pre-loaded at startup for C1..C5.
  </what-built>
  <how-to-verify>
    1. From the repo root, run the API:
       `uv run --package f1-api uvicorn f1_api.app:app --port 8000`
       Expected log lines:
         - "FastF1 cache initialized at ..."
         - "Session cleanup daemon started (thread=session-ttl-cleanup)"
         - Either "Primed posterior cache for C3" OR "Posterior prime failed for C3: ..." (both OK — depends on whether Phase 3 artifacts exist locally).

    2. GET /healthz:
       `curl http://localhost:8000/healthz`
       Expected: `{"status":"ok"}`

    3. GET /calibration/C3 (skip if no Phase 3 artifacts — you'll get 404 legitimately):
       `curl -i http://localhost:8000/calibration/C3`
       Expected: 200 with JSON containing metadata/stage1/stage2/stage3/stage4/stage5 blocks, OR 404 if Phase 3 hasn't populated the DB.
       If 200: visually confirm stage4.beta_therm has {mean, sd, hdi_lo_95, hdi_hi_95, r_hat, ess_bulk}.

    4. POST /sessions/upload with the test fixture zip (create a tiny zip first):
       From repo root:
       `python -c "from packages.api.tests.fixtures.zip_fixtures import make_valid_zip; open('/tmp/cache.zip','wb').write(make_valid_zip())"`
       Then:
       `curl -F 'file=@/tmp/cache.zip;type=application/zip' http://localhost:8000/sessions/upload`
       Expected: 200 with `{"session_id": "<32hex>", "expires_at": "<ISO8601>"}`
       Visually confirm `.data/sessions/{session_id}/` was created with the merged zip contents.

    5. Try a malicious upload to confirm security:
       `python -c "from packages.api.tests.fixtures.zip_fixtures import make_zip_slip; open('/tmp/evil.zip','wb').write(make_zip_slip())"`
       `curl -i -F 'file=@/tmp/evil.zip;type=application/zip' http://localhost:8000/sessions/upload`
       Expected: 400 with detail mentioning "escape" or "..".

    6. POST /simulate (requires Phase 3 artifacts for a real response; otherwise you'll get 404):
       `curl -X POST http://localhost:8000/simulate -H 'Content-Type: application/json' -d '{"race_id":"2023-bahrain_grand_prix","driver_code":"VER","stint_index":2}'`
       If 200: visually confirm top-level keys are metadata/per_timestep/per_lap/per_stint, and spot-check that per_lap[0].pred_s is a `{mean, lo_95, hi_95}` object (NOT a scalar).
       Confirm metadata has calibration_id, model_schema_version="v1", fastf1_version, compound, stint_index, overrides_applied, k_draws=100.
       If 404 ("no calibration for compound=..."): confirm the error message is correct; Phase 3 artifacts are simply not yet present locally. This is expected if Phase 3 hasn't been run.

    7. Confirm OpenAPI surface at runtime:
       `curl http://localhost:8000/openapi.json | python -c "import json,sys; d=json.load(sys.stdin); print(sorted(d['paths'].keys()))"`
       Expected output includes: /calibration/{compound}, /healthz, /races, /races/{race_id}/drivers, /sessions/upload, /simulate, /stints/{race_id}/{driver_id}.

    8. Confirm gzip compression on a large response (only meaningful if step 6 returned 200):
       `curl -i -H 'Accept-Encoding: gzip' -X POST http://localhost:8000/simulate -H 'Content-Type: application/json' -d '...' | head -20`
       Expected header: `Content-Encoding: gzip`. (If step 6 was 404 due to missing Phase 3, skip this.)

    9. Kill the uvicorn process (Ctrl+C). Confirm a shutdown log line: "Session cleanup daemon stop signaled".
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint — awaiting human approval. Automated verification deferred to prior tasks (pytest suite). Resume signal: approved/issue description."</automated>
  </verify>
  <done>
    User types "approved" (or equivalent) after all 9 verification steps pass locally. If Phase 3 artifacts are not present, steps 3/6/8 may legitimately 404 — approved only if the error messages are correct.
  </done>
  <resume-signal>
    Type "approved" if all steps pass (step 6 may legitimately 404 if Phase 3 artifacts aren't present — that is NOT a failure as long as the error message is correct).
    Type specific issue descriptions if anything is wrong — e.g. "step 4 returned 500 instead of 200" — and Claude will debug.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced in this plan. Tests and human checkpoint verify previously-declared boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-BENCH-FLAKE | Denial of Service (test flakiness) | pytest-benchmark wall-time assertion | mitigate | Use time.perf_counter() manually, not pytest-benchmark's statistical harness (which would auto-rerun N times). Stubbed run_simulation has deterministic timing. Assertion threshold 2.0 s is the D-04 budget, not a tight fit — has ample headroom since real per-call physics is removed. |
| T-4-E2E-LEAK | Information Disclosure | test_phase4_integration using real fastf1 cache | mitigate | All DB paths and SESSION_ROOT monkeypatched to tmp_path. No real Jolpica calls. |
</threat_model>

<verification>
1. `pytest packages/api/tests/ -v` — all unit + integration tests green.
2. `pytest packages/api/tests/test_simulate.py::test_simulate_wall_time -m integration` — wall time <2 s.
3. `pytest packages/api/tests/test_phase4_integration.py -m integration` — E2E passes.
4. Human checkpoint approved.
</verification>

<success_criteria>
- 23 Phase 4 tests green (19 per-requirement + 3 integration + 1 bonus symlink)
- D-04 wall-time budget verified (<2 s cold path on stubbed physics)
- Cross-endpoint E2E test confirms calibration_id consistency between /simulate and /calibration
- Human operator confirms the app runs locally, responds correctly, and handles both valid and malicious uploads
- D-05 enforced at runtime (no MCMC modules in sys.modules after create_app)
</success_criteria>

<output>
After completion, create `.planning/phases/04-simulation-api-uncertainty-quantification/04-05-SUMMARY.md` with: measured wall time, test-pass count, E2E flow description, and the human-verify approval note.
</output>
