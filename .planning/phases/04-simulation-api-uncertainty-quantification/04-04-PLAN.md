---
phase: 04
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - packages/api/src/f1_api/app.py
  - packages/api/src/f1_api/services/simulate.py
  - packages/api/tests/test_sessions.py
autonomous: true
requirements: [API-04, API-05, API-06]
user_setup: []
tags: [app-wiring, middleware, lifespan, session-integration]

must_haves:
  truths:
    - "create_app() includes /simulate, /calibration/{compound}, /sessions/upload in the router chain"
    - "GZipMiddleware is installed with minimum_size=1024 and compresslevel=5 (Pitfall 9)"
    - "Lifespan starts the session cleanup daemon thread and primes posterior cache for C1..C5"
    - "POST /simulate with a session_id merges the session's FastF1 cache contents into the global cache dir before load_stint runs (Pitfall 7 Option A)"
    - "A fresh TestClient produces 3 new OpenAPI paths (/simulate, /calibration/{compound}, /sessions/upload) beyond Phase 1's existing 4"
    - "Existing Phase 1 tests (test_endpoints.py) still pass"
  artifacts:
    - path: "packages/api/src/f1_api/app.py"
      provides: "Updated create_app() with 3 new routers + GZipMiddleware + extended lifespan"
    - path: "packages/api/src/f1_api/services/simulate.py"
      provides: "session_id-aware load_stint call path (merges session FastF1 cache before load_stint)"
  key_links:
    - from: "app.py lifespan"
      to: "services/sessions.py::start_cleanup_daemon"
      via: "call in lifespan async context manager"
      pattern: "start_cleanup_daemon"
    - from: "app.py lifespan"
      to: "services/posterior_store.py::prime_posterior"
      via: "loop over C1..C5, prime each (best-effort)"
      pattern: "prime_posterior"
    - from: "services/simulate.py run_simulation_with_uncertainty"
      to: "services/sessions.py::SESSION_ROOT"
      via: "if session_id provided, merge session zip contents into global FastF1 cache before load_stint"
      pattern: "SESSION_ROOT"
---

<objective>
Wave-2 wiring: install Plan 01-03 routers into the FastAPI app, add GZipMiddleware, extend the lifespan context manager to start the session cleanup daemon and prime the posterior cache, and thread `session_id` into the /simulate service so uploaded sessions actually skip Jolpica (Pitfall 7 Option A).

This plan is DEPENDENT on Plans 01, 02, 03 (all three must be merged first). It touches `app.py` (shared by all three routers) and extends `services/simulate.py` with the session-merge path.

Scope EXCLUDES:
- Integration E2E tests + wall-time benchmark (to Plan 05)
- Human-verify checkpoint (to Plan 05)

Purpose: After Wave 2, the API is fully functional end-to-end. `uvicorn f1_api.app:app` serves all 7 endpoints, compresses large responses, cleans up sessions in the background, and routes uploaded caches away from Jolpica.

Output: 2 file updates + 1 integration test unblocked (test_session_routes_simulate gains its implementation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-simulation-api-uncertainty-quantification/04-CONTEXT.md
@.planning/phases/04-simulation-api-uncertainty-quantification/04-RESEARCH.md
@packages/api/src/f1_api/app.py
@packages/api/src/f1_api/services/simulate.py
@packages/api/src/f1_api/services/sessions.py
@packages/api/src/f1_api/services/posterior_store.py
@packages/calibration/src/f1_calibration/db.py
@packages/calibration/src/f1_calibration/common.py
@packages/core/src/f1_core/ingestion/fastf1_client.py

<interfaces>
Current create_app shape (from packages/api/src/f1_api/app.py Phase 1):
- `create_app()` builds a FastAPI with lifespan=lifespan
- Calls `app.add_middleware(CORSMiddleware, ...)` once
- Calls `app.include_router(...)` three times (races, drivers, stints)
- Defines inline `@app.get("/healthz")`

Lifespan extension targets (RESEARCH Example 7):
- After init_cache(), call start_cleanup_daemon() - capture (thread, stop_event)
- Loop over ("C1","C2","C3","C4","C5") calling prime_posterior(DEFAULT_DB_PATH, c) in a try/except
- After yield, in finally block, call stop_event.set() for graceful daemon shutdown

Pitfall 7 Option A - session merge path:
- FastF1 global cache root comes from init_cache() — by default `WORKSPACE_ROOT/.data/fastf1/`
- Session zip content (per make_valid_zip in Plan 00) has top-level structure `{YEAR}/{YYYY-MM-DD_EVENT}/...`
- Merge approach: shutil.copytree(SESSION_ROOT/{session_id}/, GLOBAL_FASTF1_CACHE_ROOT, dirs_exist_ok=True) BEFORE load_stint() is invoked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend app.py lifespan + install routers + GZipMiddleware</name>
  <read_first>
    - packages/api/src/f1_api/app.py (current state - exact lifespan shape + create_app order)
    - .planning/phases/04-simulation-api-uncertainty-quantification/04-RESEARCH.md Code Example 7 (GZip + CORS + lifespan wiring spec)
    - .planning/phases/04-simulation-api-uncertainty-quantification/04-RESEARCH.md Common Pitfalls 9 (compresslevel=5 not 9)
    - packages/api/src/f1_api/services/sessions.py (start_cleanup_daemon signature)
    - packages/api/src/f1_api/services/posterior_store.py (prime_posterior signature)
    - packages/calibration/src/f1_calibration/db.py (DEFAULT_DB_PATH)
  </read_first>
  <files>packages/api/src/f1_api/app.py</files>
  <action>
    Edit `packages/api/src/f1_api/app.py` - do NOT rewrite from scratch; make targeted additions.

    **Step 1 - Add new imports** (top of file, grouped with existing imports):
    Add these lines alongside the existing f1_api.routers imports:
        from fastapi.middleware.gzip import GZipMiddleware
        from f1_calibration.db import DEFAULT_DB_PATH
        from f1_api.routers import calibration as calibration_router
        from f1_api.routers import sessions as sessions_router
        from f1_api.routers import simulate as simulate_router
        from f1_api.services.posterior_store import prime_posterior
        from f1_api.services.sessions import start_cleanup_daemon

    **Step 2 - Replace the lifespan function body** with this exact extended version. Keep the original init_cache() call. Add start_cleanup_daemon (capture thread + stop event), prime_posterior for C1..C5 inside try/except, and a finally that sets the stop event:

        @asynccontextmanager
        async def lifespan(_app: FastAPI) -> AsyncIterator[None]:
            """Phase 1 + Phase 4 startup: init FastF1 cache, start session TTL daemon,
            prime posterior cache for C1..C5 (best-effort — missing calibrations do
            not block startup)."""
            cache_dir = init_cache()
            log.info("FastF1 cache initialized at %s", cache_dir)

            cleanup_thread, cleanup_stop = start_cleanup_daemon()
            log.info("Session cleanup daemon started (thread=%s)", cleanup_thread.name)

            for compound in ("C1", "C2", "C3", "C4", "C5"):
                try:
                    prime_posterior(DEFAULT_DB_PATH, compound)
                    log.info("Primed posterior cache for %s", compound)
                except Exception as e:  # noqa: BLE001
                    log.warning("Posterior prime failed for %s: %s", compound, e)

            try:
                yield
            finally:
                cleanup_stop.set()
                log.info("Session cleanup daemon stop signaled")

    **Step 3 - Update create_app()** - add GZipMiddleware after the existing CORSMiddleware line:
        app.add_middleware(GZipMiddleware, minimum_size=1024, compresslevel=5)

    And add three new include_router calls AFTER the existing stints_router line:
        app.include_router(simulate_router.router)
        app.include_router(calibration_router.router)
        app.include_router(sessions_router.router)

    **Step 4 - Verify the GET /healthz endpoint stays intact** - do NOT delete it.

    **Compatibility note:** The lifespan already wraps prime_posterior in try/except Exception, so a missing DB file at startup logs a warning and continues. No changes to posterior_store.py needed.
  </action>
  <verify>
    <automated>cd packages/api && python -c "from f1_api.app import create_app; app=create_app(); paths={r.path for r in app.routes if hasattr(r,'path')}; expected={'/races','/races/{race_id}/drivers','/stints/{race_id}/{driver_id}','/simulate','/calibration/{compound}','/sessions/upload','/healthz'}; missing=expected-paths; assert not missing, f'missing: {missing}'; print(f'OK: {len(paths)} routes')" && pytest packages/api/tests/test_endpoints.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/api/src/f1_api/app.py` contains import of `GZipMiddleware` (grep returns 1 match)
    - File contains exactly 6 `app.include_router(` calls (3 existing + 3 new) (grep `include_router` returns 6 matches)
    - File contains `app.add_middleware(GZipMiddleware, minimum_size=1024, compresslevel=5)` (grep returns 1 match, exact text)
    - Lifespan calls `start_cleanup_daemon()` (grep returns 1 match)
    - Lifespan calls `prime_posterior(DEFAULT_DB_PATH, compound)` inside a for loop over C1..C5 (grep `prime_posterior` returns 1 match; grep `"C1", "C2", "C3", "C4", "C5"` returns 1 match)
    - Lifespan has `try: yield / finally: cleanup_stop.set()` pattern (grep `cleanup_stop.set()` returns 1 match)
    - Python smoke test (verify command) reports all 7 expected routes present
    - `pytest packages/api/tests/test_endpoints.py -x` passes (Phase 1 tests still work)
    - Existing `@app.get("/healthz")` definition is preserved (grep `/healthz` returns at least 1 match)
    - `async def` only appears for `lifespan` (grep `async def` returns 1 match)
  </acceptance_criteria>
  <done>
    The app factory installs 6 routers, applies CORS + GZip middleware, and the lifespan starts the cleanup daemon and primes posterior for C1..C5. Phase 1 tests regressions-free.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread session_id into simulate service (Pitfall 7 Option A)</name>
  <read_first>
    - packages/api/src/f1_api/services/simulate.py (current `run_simulation_with_uncertainty` signature — session_id is already accepted but unused)
    - packages/api/src/f1_api/services/sessions.py (SESSION_ROOT constant + directory layout)
    - packages/core/src/f1_core/ingestion/fastf1_client.py (init_cache returns the global FastF1 cache root Path)
    - .planning/phases/04-simulation-api-uncertainty-quantification/04-RESEARCH.md Common Pitfalls 7 (Option A = merge session contents into global cache)
    - packages/api/tests/fixtures/zip_fixtures.py (make_valid_zip — the FastF1-shaped directory structure we merge)
  </read_first>
  <files>
    packages/api/src/f1_api/services/simulate.py,
    packages/api/tests/test_sessions.py
  </files>
  <behavior>
    - `run_simulation_with_uncertainty(..., session_id="abc...")` with a valid pre-existing session dir:
      1. Resolves SESSION_ROOT/{session_id}/. Must exist; else ValueError("unknown session_id").
      2. Validates session_id via regex `^[0-9a-f]{32}$` to prevent path traversal (defence-in-depth on top of Pydantic).
      3. Copies session contents into the global FastF1 cache root via `shutil.copytree(session_dir, fastf1_cache_root, dirs_exist_ok=True)`.
      4. Calls load_stint() as normal — FastF1 finds the data locally.
    - `run_simulation_with_uncertainty(..., session_id=None)` — current behavior unchanged (load_stint hits global cache / Jolpica as needed).
    - Integration test `test_session_routes_simulate` uses the valid zip fixture, uploads it via POST /sessions/upload to get a session_id, then posts /simulate with that session_id + a monkeypatched load_stint that asserts the session contents are present in the cache dir. (We don't actually hit Jolpica in tests.)
  </behavior>
  <action>
    **Step 1 - Extend `run_simulation_with_uncertainty` in services/simulate.py:**

    Add a helper function `_merge_session_into_cache(session_id: str) -> None` near the top of the module:

        import re
        import shutil
        from f1_core.ingestion import init_cache
        from f1_api.services.sessions import SESSION_ROOT

        _SESSION_ID_RE = re.compile(r"^[0-9a-f]{32}$")

        def _merge_session_into_cache(session_id: str) -> None:
            """Pitfall 7 Option A: copy uploaded session contents into global FastF1
            cache so load_stint() finds data without a Jolpica round-trip.
            """
            if not _SESSION_ID_RE.match(session_id):
                raise ValueError(f"invalid session_id format: {session_id!r}")
            session_dir = SESSION_ROOT / session_id
            if not session_dir.exists() or not session_dir.is_dir():
                raise ValueError(f"unknown session_id: {session_id}")
            # Defence-in-depth: ensure session_dir really resolves under SESSION_ROOT
            resolved = session_dir.resolve()
            if not resolved.is_relative_to(SESSION_ROOT.resolve()):
                raise ValueError(f"session_id escapes SESSION_ROOT: {session_id}")
            fastf1_cache_root = init_cache()   # idempotent
            shutil.copytree(session_dir, fastf1_cache_root, dirs_exist_ok=True)

    Then in `run_simulation_with_uncertainty`, at the very start BEFORE `load_stint` is called, add:

        if session_id is not None:
            _merge_session_into_cache(session_id)

    (This is the ONLY change to the existing function body; keep the rest intact.)

    Remove the `# noqa: ARG001 — consumed in Plan 03` comment from the session_id parameter since it's now consumed.

    **Step 2 - Replace the Wave-0 skip body in `test_session_routes_simulate`** with real assertions:

        @pytest.mark.integration
        def test_session_routes_simulate(client, monkeypatch, tmp_path) -> None:
            """API-06-d: uploaded session routes /simulate without Jolpica."""
            # Patch SESSION_ROOT and FastF1 cache to tmp_path
            sessions_root = tmp_path / "sessions"
            fastf1_root = tmp_path / "fastf1_cache"
            sessions_root.mkdir(parents=True)
            fastf1_root.mkdir(parents=True)
            monkeypatch.setattr("f1_api.services.sessions.SESSION_ROOT", sessions_root)
            monkeypatch.setattr("f1_api.services.simulate.SESSION_ROOT", sessions_root)

            # Patch init_cache to return our tmp fastf1_root
            monkeypatch.setattr(
                "f1_api.services.simulate.init_cache",
                lambda: fastf1_root,
            )

            # Patch load_stint to assert the session contents were merged,
            # then return a minimal StintArtifact (use fake_stint_artifact fixture
            # or build one inline).
            load_stint_calls = []
            def _fake_load_stint(year, event, driver_code, stint_index):
                load_stint_calls.append((year, event, driver_code, stint_index))
                # Assert the session's top-level dir is now present under fastf1_root
                merged_dirs = list(fastf1_root.iterdir())
                assert merged_dirs, "session contents were NOT merged into global cache"
                from f1_core.ingestion.cache import StintArtifact
                import pandas as pd
                return StintArtifact(
                    year=year, event=event, driver_code=driver_code, stint_index=stint_index,
                    laps=pd.DataFrame({"Compound": ["MEDIUM"], "LapTime": [pd.Timedelta(seconds=90)]}),
                    car_data=pd.DataFrame({"Time": pd.date_range("2023-01-01", periods=4, freq="250ms"),
                                            "Speed": [100, 110, 120, 115]}),
                    pos_data=pd.DataFrame(),
                    weather=pd.DataFrame(),
                    telemetry_hash="fake",
                )
            monkeypatch.setattr("f1_api.services.simulate.load_stint", _fake_load_stint)

            # Also stub the posterior/params layer so we don't need a real calibration:
            # (reuse whatever stubs test_simulate_happy_path sets up; copy-paste the
            # setup function into conftest.py as a helper if needed.)

            # 1. Upload a zip via POST /sessions/upload
            from packages.api.tests.fixtures.zip_fixtures import make_valid_zip
            upload_resp = client.post(
                "/sessions/upload",
                files={"file": ("cache.zip", make_valid_zip(), "application/zip")},
            )
            assert upload_resp.status_code == 200, upload_resp.text
            session_id = upload_resp.json()["session_id"]

            # 2. POST /simulate with that session_id
            #    (this will exercise _merge_session_into_cache + _fake_load_stint)
            # ... full setup is complex; at minimum verify the merge happens:
            from f1_api.services.simulate import _merge_session_into_cache
            _merge_session_into_cache(session_id)
            merged = list(fastf1_root.iterdir())
            assert merged, "merge did not populate fastf1_root"

    The full POST /simulate path inside this test is heavy to stub (needs posterior + params); if the simpler assertion above is not enough to cover API-06-d, extend by adding the same monkeypatches used in `test_simulate_cache_hit` (Plan 01 Task 5) and then POST /simulate with session_id and assert status 200.
  </action>
  <verify>
    <automated>cd packages/api && pytest tests/test_sessions.py::test_session_routes_simulate -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api/src/f1_api/services/simulate.py` contains `_merge_session_into_cache` function (grep `^def _merge_session_into_cache` returns 1 match)
    - The merge function calls `shutil.copytree(session_dir, fastf1_cache_root, dirs_exist_ok=True)` (grep `dirs_exist_ok=True` returns ≥1 match)
    - session_id regex validated in the merge function (grep `_SESSION_ID_RE` returns ≥1 match)
    - `run_simulation_with_uncertainty` calls `_merge_session_into_cache(session_id)` when session_id is truthy (grep returns 1 match)
    - `test_session_routes_simulate` no longer contains `pytest.skip` as its body (grep in that function body for `pytest.skip` returns 0)
    - `test_session_routes_simulate` is still marked `@pytest.mark.integration`
    - Running `pytest tests/test_sessions.py::test_session_routes_simulate -m integration` passes
    - Security defence check: grep `is_relative_to` in the simulate.py session-merge code returns ≥1 match
  </acceptance_criteria>
  <done>
    /simulate with a session_id merges the session dir into the global FastF1 cache before load_stint runs. test_session_routes_simulate is green under @pytest.mark.integration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client session_id → filesystem | session_id regex-validated at Pydantic layer (Plan 01) AND re-validated in _merge_session_into_cache (defence-in-depth) + is_relative_to guard against symlink-based path escapes. |
| lifespan → external state | prime_posterior failures do NOT block startup (wrapped in try/except Exception). |
| cleanup daemon → SESSION_ROOT | Only touches directories directly under SESSION_ROOT; TTL-based removal via shutil.rmtree(ignore_errors=True). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SESSION-ESCAPE | Tampering | _merge_session_into_cache | mitigate | Regex validate session_id, is_relative_to SESSION_ROOT check, explicit existence check. Bypass requires the uploader to have already created a malicious session dir (blocked by Plan 03's extract_session_zip). |
| T-4-LIFESPAN-CRASH | Denial of Service | lifespan startup | mitigate | prime_posterior wrapped in try/except; missing DB / NetCDF only logs warning. Cleanup daemon uses daemon=True so process exit is clean. |
| T-4-MERGE-COLLISION | Tampering (overwrite) | shutil.copytree dirs_exist_ok=True | accept | Session contents overwrite global cache entries with same relative path. This is intentional per Pitfall 7 Option A (session IS the authoritative cache for that session). Risk: user uploads a crafted "authentic" cache that poisons future requests. Accepted in V1 — no auth; mitigate by documenting that uploads taint the global cache for TTL duration. |
| T-4-PYMC | D-05 runtime violation | app.py imports | mitigate | app.py imports only f1_api.routers.* and f1_api.services.{posterior_store, sessions} — all D-05 clean. Baseline `test_no_mcmc_at_runtime` (Plan 00) enforces. |
</threat_model>

<verification>
1. `pytest packages/api/tests/ -x -m "not integration"` — all unit tests green (Phase 1 + Phase 4 Plans 00-03).
2. `pytest packages/api/tests/test_sessions.py::test_session_routes_simulate -m integration` — passes.
3. Smoke test: `python -c "from f1_api.app import create_app; app=create_app(); print(len(app.routes))"` — prints at least 7.
4. `grep -rn "import pymc\|import numpyro\|import pytensor" packages/api/src/` — 0 matches.
5. `pytest packages/api/tests/ -k test_no_mcmc_at_runtime` — still green.
</verification>

<success_criteria>
- app.py wires 3 new routers + GZipMiddleware + extended lifespan
- test_session_routes_simulate turns green (API-06-d)
- Phase 1 endpoint tests unaffected
- No PyMC imports introduced
- All 17 of 18 unit-testable Phase 4 tests green (wall_time benchmark remains for Plan 05)
</success_criteria>

<output>
Create `.planning/phases/04-simulation-api-uncertainty-quantification/04-04-SUMMARY.md` with: the six include_router order, the GZip config (minimum_size=1024, compresslevel=5), lifespan prime_posterior list, cleanup daemon name, and updated test-pass count.
</output>
