---
phase: 05-dashboard-shell-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/biome.json
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/styles/global.css
  - frontend/src/test/setup.ts
  - frontend/src/mocks/server.ts
  - frontend/src/mocks/handlers.ts
  - frontend/src/lib/scales.test.ts
  - frontend/src/lib/track.test.ts
  - frontend/src/lib/formatters.test.ts
  - frontend/src/stores/useUIStore.test.ts
  - frontend/src/lib/sse.test.ts
  - frontend/public/fonts/JetBrainsMono-Regular.woff2
  - frontend/public/fonts/JetBrainsMono-Medium.woff2
  - frontend/public/fonts/JetBrainsMono-Bold.woff2
  - frontend/public/fonts/Inter-Regular.woff2
  - frontend/public/fonts/Inter-Medium.woff2
autonomous: true
requirements:
  - DASH-04
  - VIZ-06

must_haves:
  truths:
    - "Running `npm run test --prefix frontend` from the repo root exits 0"
    - "global.css contains all CSS custom property tokens (--bg, --accent, --rule, etc.)"
    - "JetBrains Mono and Inter fonts load from /fonts/ — no Google CDN requests in production build"
    - "body has font-family: var(--mono) and font-feature-settings tabular figures"
    - "No rounded corners anywhere in global styles"
    - "Biome lint passes with no errors on the scaffolded files"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vite 8 + React 19 + TypeScript + Tailwind 4 + Biome + Vitest + MSW + Zustand + TanStack Query + D3 subpackages"
    - path: "frontend/src/styles/global.css"
      provides: "All CSS custom property tokens and @theme block"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest config with jsdom + MSW setup"
    - path: "frontend/src/mocks/server.ts"
      provides: "MSW Node server for unit tests"
    - path: "frontend/src/lib/scales.test.ts"
      provides: "Stub tests for VIZ-02 and VIZ-06"
    - path: "frontend/src/lib/sse.test.ts"
      provides: "Stub test for DASH-03"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/src/styles/global.css"
      via: "@tailwindcss/vite plugin auto-discovers CSS entry"
      pattern: "tailwindcss\\(\\)"
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "setupFiles"
      pattern: "setupFiles.*setup"
---

<objective>
Scaffold the `frontend/` package from scratch: Vite 8 + React 19 + TypeScript 6 + Tailwind 4 + Biome + all design tokens as CSS custom properties + self-hosted fonts + Wave 0 test infrastructure (Vitest 2, MSW 2, stub test files).

Purpose: Wave 0 infra gate — every subsequent panel plan depends on this foundation. No panel can be built until the CSS tokens, test runner, and mock server are in place.

Output: A runnable frontend with `npm run dev` (blank page is fine), `npm run test` exits 0 (stubs pass trivially), `npm run lint` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-shell-visualization/05-CONTEXT.md
@.planning/phases/05-dashboard-shell-visualization/05-RESEARCH.md
@.planning/design_reference/design_handoff_f1_cockpit/README.md
</context>

<interfaces>
<!-- Design token contract — executor must use these exact values -->
<!-- Source: .planning/design_reference/design_handoff_f1_cockpit/README.md §Design Tokens -->

CSS Custom Properties (paste verbatim into :root {}):
```css
--bg:             #05070b;
--panel:          #0a0e15;
--panel-bg:       #070a11;
--panel-header:   #0c1119;
--panel-header-hi:#111827;
--rule:           #1a2130;
--rule-strong:    #2a3445;
--text:           #e8eef7;
--text-dim:       #6a7788;
--text-muted:     #46525f;
--accent:         #00E5FF;
--accent-dim:     #0092a8;
--hot:            #FF3344;
--warn:           #FFB020;
--ok:             #22E27A;
--purple:         #A855F7;
--ferrari:        #DC0000;
--mono:           'JetBrains Mono', ui-monospace, monospace;
--sans:           'Inter', ui-sans-serif, sans-serif;
```

Tailwind @theme tokens (in @theme {} block, separate from :root):
```css
--color-bg: #05070b;
--color-panel: #0a0e15;
--color-panel-bg: #070a11;
--color-panel-header: #0c1119;
--color-panel-header-hi: #111827;
--color-rule: #1a2130;
--color-rule-strong: #2a3445;
--color-text: #e8eef7;
--color-text-dim: #6a7788;
--color-text-muted: #46525f;
--color-accent: #00E5FF;
--color-accent-dim: #0092a8;
--color-hot: #FF3344;
--color-warn: #FFB020;
--color-ok: #22E27A;
--color-purple: #A855F7;
--font-mono: 'JetBrains Mono', ui-monospace, monospace;
--font-sans: 'Inter', ui-sans-serif, sans-serif;
```

FIA compound colors (for scales.ts):
```typescript
SOFT: '#FF3333', MEDIUM: '#FFD700', HARD: '#FFFFFF', INTER: '#22C55E', WET: '#3B82F6'
```

Okabe-Ito corner colors (for scales.ts):
```typescript
fl: '#E69F00', fr: '#56B4E9', rl: '#009E73', rr: '#F0E442'
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Vite scaffold + Tailwind 4 + Biome + CSS design tokens + self-hosted fonts</name>
  <files>
    frontend/package.json
    frontend/index.html
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/biome.json
    frontend/src/styles/global.css
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/public/fonts/ (download woff2 files here)
  </files>
  <read_first>
    - .planning/phases/05-dashboard-shell-visualization/05-RESEARCH.md (Pattern 1: Tailwind 4, Pitfall 1, Pitfall 2, Pitfall 7)
    - .planning/design_reference/design_handoff_f1_cockpit/README.md (§Design Tokens — exact CSS values)
    - .planning/phases/05-dashboard-shell-visualization/05-CONTEXT.md (D-05 Biome, locked)
  </read_first>
  <action>
Create the `frontend/` directory at repo root (alongside `packages/`).

Run: `npm create vite@latest frontend -- --template react-ts` from the repo root.

Then in `frontend/` install all dependencies:
```bash
npm install react react-dom
npm install @tanstack/react-query zustand msw
npm install d3-scale d3-shape d3-array d3-axis d3-interpolate d3-scale-chromatic
npm install -D typescript @vitejs/plugin-react
npm install -D tailwindcss @tailwindcss/vite
npm install -D @biomejs/biome vitest @vitest/ui jsdom
npm install -D @types/d3-scale @types/d3-shape @types/d3-array @types/d3-axis @types/d3-interpolate @types/d3-scale-chromatic
```

Versions: react@19.2.5, typescript@6.0.3, vite@8.0.10, tailwindcss@4.2.4, @tailwindcss/vite@4.2.4, @biomejs/biome@2.4.13, zustand@5.0.12, @tanstack/react-query@5.100.1, msw@2.13.5, vitest@2.1.9.

**vite.config.ts** (EXACT):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: { proxy: { '/api': 'http://localhost:8000' } },
})
```

**DO NOT create tailwind.config.js** — Tailwind 4 is CSS-first; the vite plugin handles discovery.

**tsconfig.json** — strict mode mandatory:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**biome.json** (EXACT):
```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.13/schema.json",
  "organizeImports": { "enabled": true },
  "linter": {
    "enabled": true,
    "rules": { "recommended": true }
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "javascript": {
    "formatter": { "quoteStyle": "single", "trailingCommas": "es5" }
  }
}
```

Add to package.json scripts:
```json
"lint": "biome lint src",
"format": "biome format --write src",
"test": "vitest run",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage"
```

**src/styles/global.css** — EXACT tokens, self-hosted fonts, no CDN in production:
```css
@import "tailwindcss";

@theme {
  --font-mono: 'JetBrains Mono', ui-monospace, monospace;
  --font-sans: 'Inter', ui-sans-serif, sans-serif;
  --color-bg: #05070b;
  --color-panel: #0a0e15;
  --color-panel-bg: #070a11;
  --color-panel-header: #0c1119;
  --color-panel-header-hi: #111827;
  --color-rule: #1a2130;
  --color-rule-strong: #2a3445;
  --color-text: #e8eef7;
  --color-text-dim: #6a7788;
  --color-text-muted: #46525f;
  --color-accent: #00E5FF;
  --color-accent-dim: #0092a8;
  --color-hot: #FF3344;
  --color-warn: #FFB020;
  --color-ok: #22E27A;
  --color-purple: #A855F7;
}

/* CSS custom properties for SVG and inline style access */
:root {
  --bg: #05070b;
  --panel: #0a0e15;
  --panel-bg: #070a11;
  --panel-header: #0c1119;
  --panel-header-hi: #111827;
  --rule: #1a2130;
  --rule-strong: #2a3445;
  --text: #e8eef7;
  --text-dim: #6a7788;
  --text-muted: #46525f;
  --accent: #00E5FF;
  --accent-dim: #0092a8;
  --hot: #FF3344;
  --warn: #FFB020;
  --ok: #22E27A;
  --purple: #A855F7;
  --ferrari: #DC0000;
  --mono: 'JetBrains Mono', ui-monospace, monospace;
  --sans: 'Inter', ui-sans-serif, sans-serif;
}

/* Self-hosted fonts — production: no Google CDN dependency */
@font-face {
  font-family: 'JetBrains Mono';
  src: url('/fonts/JetBrainsMono-Regular.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}
@font-face {
  font-family: 'JetBrains Mono';
  src: url('/fonts/JetBrainsMono-Medium.woff2') format('woff2');
  font-weight: 500;
  font-style: normal;
  font-display: swap;
}
@font-face {
  font-family: 'JetBrains Mono';
  src: url('/fonts/JetBrainsMono-Bold.woff2') format('woff2');
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}
@font-face {
  font-family: 'Inter';
  src: url('/fonts/Inter-Regular.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}
@font-face {
  font-family: 'Inter';
  src: url('/fonts/Inter-Medium.woff2') format('woff2');
  font-weight: 500;
  font-style: normal;
  font-display: swap;
}

*, *::before, *::after {
  box-sizing: border-box;
  border-radius: 0 !important;  /* NO rounded corners anywhere — design lock */
}

body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--mono);
  font-feature-settings: "tnum" 1, "ss01" 1;
  min-width: 1600px;
  overflow-x: auto;
}

/* Keyframe animations from design reference */
@keyframes blink-red {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}
@keyframes pulse-dot {
  0% { box-shadow: 0 0 0 0 rgba(0, 229, 255, 0.55); }
  100% { box-shadow: 0 0 0 8px rgba(0, 229, 255, 0); }
}
```

Download JetBrains Mono and Inter woff2 files to `frontend/public/fonts/`:
- JetBrainsMono-Regular.woff2, JetBrainsMono-Medium.woff2, JetBrainsMono-Bold.woff2 — from https://github.com/JetBrains/JetBrainsMono/releases/latest
- Inter-Regular.woff2, Inter-Medium.woff2 — from https://github.com/rsms/inter/releases/latest

Use `curl -L -o frontend/public/fonts/JetBrainsMono-Regular.woff2 "URL"` for each. Verify each file is >50KB (valid woff2, not an error page).

**index.html** — no Google Fonts link tag, fonts load from /fonts/:
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>F1 Tire Degradation Analyzer</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**src/main.tsx** — minimal, no MSW in main.tsx yet (that comes in Plan 09 wiring):
```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import './styles/global.css'
import App from './App'

const queryClient = new QueryClient()

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
    </QueryClientProvider>
  </React.StrictMode>
)
```

**src/App.tsx** — placeholder shell (full grid in Plan 09):
```typescript
export default function App() {
  return (
    <div style={{ color: 'var(--text)', fontFamily: 'var(--mono)', padding: 16 }}>
      F1 Tire Degradation Analyzer — shell loading…
    </div>
  )
}
```

Initialize MSW service worker (for browser use in Plan 09):
```bash
npx msw init frontend/public/ --save
```
This creates `frontend/public/mockServiceWorker.js`.
  </action>
  <verify>
    <automated>cd "C:/Users/Eason/Desktop/CC/F1 Dashboard/frontend" && npm run lint 2>&1 | tail -5; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json exists and contains "vite": "^8" (or exact 8.0.10)
    - frontend/package.json contains "@tailwindcss/vite" and NOT "@tailwindcss/postcss"
    - frontend/vite.config.ts contains "tailwindcss()" and NOT "tailwind.config"
    - frontend/src/styles/global.css contains "--bg: #05070b"
    - frontend/src/styles/global.css contains "--accent: #00E5FF"
    - frontend/src/styles/global.css contains "--rule: #1a2130"
    - frontend/src/styles/global.css contains "--panel-header-hi: #111827"
    - frontend/src/styles/global.css contains "font-feature-settings"
    - frontend/src/styles/global.css contains "border-radius: 0 !important"
    - frontend/src/styles/global.css contains "url('/fonts/JetBrainsMono-Regular.woff2')"
    - frontend/src/styles/global.css does NOT contain "fonts.googleapis.com"
    - frontend/index.html does NOT contain "fonts.googleapis.com"
    - frontend/public/fonts/JetBrainsMono-Regular.woff2 exists and is >50KB
    - frontend/public/fonts/Inter-Regular.woff2 exists and is >50KB
    - frontend/public/mockServiceWorker.js exists
    - frontend/biome.json contains "biomejs.dev/schemas/2.4.13"
    - NO frontend/tailwind.config.js file exists
    - `npm run lint --prefix frontend` exits 0
    - `npm run build --prefix frontend` exits 0 (blank App builds clean)
  </acceptance_criteria>
  <done>Vite scaffold is runnable, Biome lint passes, no Google CDN font dependency, all CSS tokens present at exact values per design lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test infrastructure — Vitest config + MSW server + stub test files</name>
  <files>
    frontend/vitest.config.ts
    frontend/src/test/setup.ts
    frontend/src/mocks/server.ts
    frontend/src/mocks/handlers.ts
    frontend/src/mocks/fixtures/bahrain-lec-s1.ts
    frontend/src/lib/scales.test.ts
    frontend/src/lib/track.test.ts
    frontend/src/lib/formatters.test.ts
    frontend/src/stores/useUIStore.test.ts
    frontend/src/lib/sse.test.ts
  </files>
  <read_first>
    - .planning/phases/05-dashboard-shell-visualization/05-VALIDATION.md (Wave 0 Requirements section)
    - .planning/phases/05-dashboard-shell-visualization/05-RESEARCH.md (Pattern 8: MSW 2.x Handlers, Vitest Configuration with MSW section)
    - .planning/design_reference/design_handoff_f1_cockpit/design/data.jsx (fixture data shape — LapData schema)
  </read_first>
  <behavior>
    - scales.test.ts: `tempToViridis(60)` returns a string starting with "rgb(" (blue/purple end of viridis); `COMPOUND_COLORS.SOFT === '#FF3333'`; `CORNER_COLORS.fl === '#E69F00'`; `COMPOUND_COLORS.MEDIUM === '#FFD700'`; `COMPOUND_COLORS.HARD === '#FFFFFF'`
    - track.test.ts: `normalizeTrackPoints([[0,0],[10,5],[20,0]])` returns array where all values are in [0,1]; empty input returns []; single point returns [[0,0]]
    - formatters.test.ts: `fmtLapTime(93.851)` returns "1:33.851"; `fmtLapTime(NaN)` returns "—:—.—"; `fmtDelta(0.123)` returns "+0.123"; `fmtDelta(-0.123)` returns "–0.123"
    - useUIStore.test.ts: initial hoveredLap is null; after setHoveredLap(5) hoveredLap equals 5; after setHoveredLap(null) hoveredLap equals null
    - sse.test.ts: stub test that skips (it.skip) — real SSE behavior tested in Plan 09
  </behavior>
  <action>
**vitest.config.ts** (EXACT — Wave 0 gap from VALIDATION.md):
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

**src/test/setup.ts**:
```typescript
import { beforeAll, afterEach, afterAll } from 'vitest'
import { server } from '../mocks/server'

beforeAll(() => server.listen({ onUnhandledRequest: 'warn' }))
afterEach(() => server.resetHandlers())
afterAll(() => server.close())
```

**src/mocks/server.ts** (MSW Node server for Vitest):
```typescript
import { setupServer } from 'msw/node'
import { handlers } from './handlers'

export const server = setupServer(...handlers)
```

**src/mocks/handlers.ts** (MSW 2.x — use http.* not rest.*):
```typescript
import { http, HttpResponse } from 'msw'

const RACES_FIXTURE = [
  { id: '2024_bahrain', name: 'Bahrain Grand Prix', round: 1, season: 2024, circuit: 'Bahrain International Circuit' },
]

const DRIVERS_FIXTURE = [
  { code: 'LEC', name: 'C. Leclerc', team: 'Ferrari', teamColor: '#DC0000' },
]

const STINTS_FIXTURE = [
  { id: 1, compound: 'MEDIUM', startLap: 1, endLap: 22, lapCount: 22, startAge: 0 },
]

export const handlers = [
  http.get('/api/races', () => HttpResponse.json(RACES_FIXTURE)),
  http.get('/api/races/:raceId/drivers', () => HttpResponse.json(DRIVERS_FIXTURE)),
  http.get('/api/stints/:raceId/:driverCode', () => HttpResponse.json(STINTS_FIXTURE)),
  http.post('/api/simulate/stream', async () => {
    const MODULES = ['Kinematics', 'Load Transfer', 'Slip & Contact', 'Force Generation', 'Thermal', 'Wear & Grip Evolution', 'Monte Carlo Roll-up']
    const stream = new ReadableStream({
      async start(controller) {
        for (let i = 0; i < 7; i++) {
          await new Promise(r => setTimeout(r, 10))
          const evt = `event: module_complete\ndata: ${JSON.stringify({ module: i + 1, name: MODULES[i], lap_count: 22 })}\n\n`
          controller.enqueue(new TextEncoder().encode(evt))
        }
        // Final event: minimal valid SimulationResult (full fixture in bahrain-lec-s1.ts)
        const finalEvt = `event: simulation_complete\ndata: ${JSON.stringify({ meta: { race: { id: '2024_bahrain', name: 'Bahrain Grand Prix', round: 1, season: 2024, circuit: 'Bahrain International Circuit' }, driver: { code: 'LEC', number: 16, name: 'C. Leclerc', team: 'Ferrari', teamColor: '#DC0000' }, stint: { id: 1, compound: 'MEDIUM', compoundColor: '#FFD700', startLap: 1, endLap: 22, lapCount: 22, startAge: 0 }, calibration_id: 1, model_schema_version: '1.0.0', fastf1_version: '3.8.2', run_id: 'test' }, laps: [], track: [[0.5, 0.88]], sectorBounds: [[0, 86], [86, 172], [172, 258]], turns: [{ n: 1, at: 0.13 }] })}\n\n`
        controller.enqueue(new TextEncoder().encode(finalEvt))
        controller.close()
      },
    })
    return new HttpResponse(stream, { headers: { 'Content-Type': 'text/event-stream' } })
  }),
]
```

**src/mocks/fixtures/bahrain-lec-s1.ts** — 22-lap fixture matching Phase 4 response schema. Build each lap from the same deterministic formula as `data.jsx::buildLaps()`:
```typescript
import type { SimulationResult } from '../lib/types'

function hash(n: number): number {
  const s = Math.sin(n * 9301 + 49297) * 233280
  return s - Math.floor(s)
}

function ci(mean: number, sigma: number) {
  return {
    mean: Number(mean.toFixed(3)),
    lo_95: Number((mean - 1.96 * sigma).toFixed(3)),
    hi_95: Number((mean + 1.96 * sigma).toFixed(3)),
  }
}

// ... (build 22 laps matching data.jsx schema, port buildLaps() to TypeScript)
export const BAHRAIN_LEC_S1: SimulationResult = { /* ... */ }
```

Create stub test files — these contain failing/skipped tests that subsequent plans will fill in. The stubs must be valid TypeScript so `vitest run` finds 0 errors:

**src/lib/scales.test.ts**:
```typescript
import { describe, it, expect } from 'vitest'

// These imports will be created in Plan 02
// import { tempToViridis, COMPOUND_COLORS, CORNER_COLORS } from './scales'

describe('scales (Wave 0 stubs — implementations land in Plan 02)', () => {
  it.todo('tempToViridis(60) returns a string starting with rgb( — blue/violet end of viridis')
  it.todo('tempToViridis(120) returns a string starting with rgb( — yellow end of viridis')
  it.todo('COMPOUND_COLORS.SOFT === "#FF3333"')
  it.todo('COMPOUND_COLORS.MEDIUM === "#FFD700"')
  it.todo('COMPOUND_COLORS.HARD === "#FFFFFF"')
  it.todo('CORNER_COLORS.fl === "#E69F00"')
  it.todo('CORNER_COLORS.fr === "#56B4E9"')
  it.todo('CORNER_COLORS.rl === "#009E73"')
  it.todo('CORNER_COLORS.rr === "#F0E442"')
})
```

**src/lib/track.test.ts**:
```typescript
import { describe, it } from 'vitest'

describe('track (Wave 0 stubs — implementations land in Plan 05)', () => {
  it.todo('normalizeTrackPoints maps GPS coords so all values are in [0,1]')
  it.todo('normalizeTrackPoints handles empty input, returns []')
  it.todo('trackToSvgPath returns string starting with M for non-empty input')
  it.todo('smoothMovingAverage window=7 reduces noise without shifting point count')
})
```

**src/lib/formatters.test.ts**:
```typescript
import { describe, it } from 'vitest'

describe('formatters (Wave 0 stubs — implementations land in Plan 02)', () => {
  it.todo('fmtLapTime(93.851) returns "1:33.851"')
  it.todo('fmtLapTime(NaN) returns "—:—.—"')
  it.todo('fmtDelta(0.123) returns "+0.123"')
  it.todo('fmtDelta(-0.123) returns "–0.123"')
  it.todo('fmtCI({mean:95,lo_95:88,hi_95:102}) returns formatted string with ±')
})
```

**src/stores/useUIStore.test.ts**:
```typescript
import { describe, it } from 'vitest'

describe('useUIStore (Wave 0 stubs — store implementation lands in Plan 02)', () => {
  it.todo('initial hoveredLap is null')
  it.todo('setHoveredLap(5) sets hoveredLap to 5')
  it.todo('setHoveredLap(null) resets hoveredLap to null')
  it.todo('initial hoveredCorner is null')
  it.todo('setHoveredCorner("fl") sets hoveredCorner to "fl"')
})
```

**src/lib/sse.test.ts**:
```typescript
import { describe, it } from 'vitest'

describe('sse (Wave 0 stubs — SSE implementation lands in Plans 08+09)', () => {
  it.todo('module_complete events update simulation store moduleProgress')
  it.todo('simulation_complete event calls setSimulationData with parsed payload')
  it.todo('fetch error sets store error state and clears loading')
  it.todo('AbortError does not set store error state')
})
```
  </action>
  <verify>
    <automated>cd "C:/Users/Eason/Desktop/CC/F1 Dashboard/frontend" && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/vitest.config.ts contains "environment: 'jsdom'"
    - frontend/vitest.config.ts contains "setupFiles"
    - frontend/src/test/setup.ts exists and contains "server.listen"
    - frontend/src/mocks/server.ts exists and contains "setupServer"
    - frontend/src/mocks/handlers.ts exists and contains "http.get" (MSW v2 API, not "rest.get")
    - frontend/src/lib/scales.test.ts exists and contains "it.todo"
    - frontend/src/lib/track.test.ts exists and contains "it.todo"
    - frontend/src/lib/formatters.test.ts exists and contains "it.todo"
    - frontend/src/stores/useUIStore.test.ts exists and contains "it.todo"
    - frontend/src/lib/sse.test.ts exists and contains "it.todo"
    - `npm run test --prefix frontend` exits 0 (stubs produce 0 failures, only todo count)
    - `npm run test --prefix frontend` output does NOT contain "FAIL" in red
  </acceptance_criteria>
  <done>All 9 Wave 0 test infrastructure files exist, `vitest run` exits 0, stub tests are valid TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → browser | Vite bundles source; font files served from /public/fonts/ |
| browser → /api | All API calls proxied through Vite dev server in development |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | global.css / font loading | mitigate | Self-host JetBrains Mono and Inter from /public/fonts/ — no Google CDN dependency in production; verified by absence of fonts.googleapis.com in index.html and global.css |
| T-05-01-02 | Spoofing | Vite dev proxy (/api) | accept | Dev-only proxy; production uses VITE_API_URL env var pointing to Fly.io backend. CORS is enforced server-side (Phase 7) |
| T-05-01-03 | Tampering | MSW handlers | accept | MSW intercepts only in development/test; service worker scope is localhost; no production traffic routed through MSW |
| T-05-01-04 | Tampering | Biome lint config | accept | Biome enforces no-eval, no-dangerouslySetInnerHTML rules by default via recommended ruleset |
</threat_model>

<verification>
After both tasks complete:

1. `npm run build --prefix frontend` exits 0 — Vite builds clean
2. `npm run test --prefix frontend` exits 0 — Vitest finds no failures (only todos)
3. `npm run lint --prefix frontend` exits 0 — Biome passes
4. `grep -r "fonts.googleapis.com" frontend/` returns empty — no CDN dependency
5. `grep --count "border-radius: 0" frontend/src/styles/global.css` returns 1 — no rounded corners
6. `ls frontend/public/fonts/*.woff2 | wc -l` returns 5 — all font files present
7. `ls frontend/public/mockServiceWorker.js` exists — MSW browser worker initialized
</verification>

<success_criteria>
- `npm run test --prefix frontend` exits 0 with 0 failures (only todos)
- `npm run build --prefix frontend` exits 0
- `npm run lint --prefix frontend` exits 0
- All 17 CSS custom properties are in global.css at exact design-locked values
- No Google Fonts CDN reference anywhere in the project
- No tailwind.config.js file exists
- 9 Wave 0 test stub files exist
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-shell-visualization/05-01-SUMMARY.md`
</output>
